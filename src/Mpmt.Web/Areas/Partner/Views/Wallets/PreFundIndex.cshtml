@using Mpmt.Core.Dtos.PrefundRequest
@using Mpmt.Core.Dtos.Paging
@using Mpmt.Core.Models.RMP;
@model PagedList<PreFundRequestDetails>
@{
    var actors = ViewBag.actions as IEnumerable<ActionPermission> ?? new List<ActionPermission>();
}
<div class="page-content">
    <div class="container-fluid">
        <!-- Page-Title -->
        <div class="row">
            <div class="col-sm-12">
                <div class="page-title-box">
                    <div class="row">
                        <div class="col">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item "><a href="javascript:void(0);">Global Setting</a></li>
                                <li class="breadcrumb-item active">Prefund request </li>
                            </ol>
                        </div><!--end col-->
                        <div class="col-auto">
                            <form id="formprefundfilter" class="row row-cols-lg-auto align-items-end">
                              @*  <div class="col-12  mb-3">
                                    <label class="visually-hidden" for="inlineFormInputGroupUsername">Partner Code</label>
                                    <div class="input-group">
                                        <input type="text" name="PartnerCode" class="form-control" id="inlineFormInputGroupUsername" placeholder="Partner Code">
                                    </div>
                                </div>*@
                                <div class="col-12">
                                    <label class="form-label mb-0" for="inlineFormSelectPref">Start Date</label>
                                    <input type="date" name="StartDate" class="form-control">
                                </div>
                                <div class="col-12">
                                    <label class="form-label mb-0" for="inlineFormSelectPref">End Date</label>
                                    <input type="date" name="EndDate" class="form-control">
                                </div>
                                @*<div class="col-12">
                                    <label class="form-label mb-0" for="inlineFormSelectPref">Transaction Id</label>
                                    <div class="input-group">
                                        <input type="text" name="TxnId" class="form-control" id="inlineFormInputGroupUsername" placeholder="Transaction id">
                                    </div>
                                </div>*@
                                <div class="col-12">
                                    <label class="form-label mb-0" for="inlineFormSelectPref">Status</label>
                                    <div class="input-group">
                                        <select id="destinationCurrency" name="Status" class="form-select form-select-md align-middle">
                                            @foreach (var item in ViewBag.statusListDdl)
                                            {
                                                <option value="@item.Value">@item.Text</option>
                                            }
                                        </select>
                                    </div>
                                </div>

                                <div class="col-12">
                                    <button id="btnprefundfilter" type="button" class="btn btn-primary"><i class="las la-filter ms-1"></i> Filter</button>
                                </div>
                            </form>
                        </div>
                    </div>

                </div><!--end page-title-box-->
            </div><!--end col-->
        </div><!--end row--><!-- end page title end breadcrumb -->
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <div class="row align-items-center">
                            <div class="col">
                                <h4 class="card-title">Prefund List</h4>
                            </div>
                            <div class="col-auto">
                         @*   <div class="add-btn">
                                <a asp-action="PreFundRequestApprovedIndex" asp-controller="PreFundRequest" class="btn btn-primary btn-shadow-2 btn-sm text-white">
                                    <i class="las la-redo ms-1"></i> PreFund Request Approved
                                </a>
                            </div>*@
                        </div>
                            <!--end col-->
                            <!--end col-->
                        </div>
                    </div>
                    <div class="card-body overflow-x-auto">
                        <partial name="_PaginationHeader" />
                        <div id="PreFundTableWrapper" class="table-responsive">
                            <partial name="_PreFundList" model="@Model" />
                        </div>
                    </div>
                </div>
            </div> 
        </div>
    </div>
</div>
@section Scripts {
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
    <script src="~/js/admin.pagination.js" asp-append-version="true"></script>

    <script src="~/js/admin.users.js" asp-append-version="true"></script>
    <script>
        $(document).ready(function() {
            // Initialize pagination settings object
            Pagination.init({
                ajax: true,
                method: 'GET',
                browserPath: '@Context.Request.Path',
                fetchUrl: '@Url.Action("PreFundIndex", "Wallets")',
                selector: '#PreFundTableWrapper'
            });

            $(Pagination).on('before', function(e) {
                e.preventDefault();
                { debugger }
                var usersFilterFormValues = $('#formprefundfilter').serializeArray();
                $.each(usersFilterFormValues, function(i, obj) {
                    e.payload.paramsBuilder.addParameter(obj['name'], obj['value']);
                });

                e.payload.paramsBuilder.addParameters(getPaginationFieldParams());
            });

            addUsersPageHandlers();

            addPaginationHandlers();
            function addUsersPageHandlers() {

                /* Custom Handlers */
                $('#formprefundfilter').on('submit', function(e) {
                    e.preventDefault();
                    e.stopPropagation();
                })

                $('#btnprefundfilter').on('click', function(e) {
                    e.preventDefault();
                    Pagination.getData();
                    return false;
                });
            }
        });
    </script>


}